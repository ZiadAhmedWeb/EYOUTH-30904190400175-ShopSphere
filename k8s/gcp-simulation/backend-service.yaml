apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: gcp-simulation
  labels:
    app: zeecrumb-backend
    cloud-simulation: gcp
spec:
  type: ClusterIP
  selector:
    app: zeecrumb-backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
